---
page_title: "samsungcloudplatformv2_scr_images Data Source - samsungcloudplatformv2"
subcategory: Container Registry
description: |-
  Get a list of Docker image IDs in a Container Registry repository.
---

# samsungcloudplatformv2_scr_images (Data Source)

Get a list of Docker image IDs in a Container Registry repository.

## Example Usage

```terraform
provider "samsungcloudplatformv2" {
}

data "samsungcloudplatformv2_scr_images" "images" {
  repository_id = var.repository_id
  name          = var.name
  sort          = var.sort
  page          = var.page
  size          = var.size
}


output "images" {
  description = "The images data source."
  value       = data.samsungcloudplatformv2_scr_images.images
}


variable "repository_id" {
  type        = string
  default     = "ENTER YOUR RESOURCE'S REPOSITORY_ID"
  description = "The ID of the repository to list images from."
}

variable "name" {
  type        = string
  default     = ""
  description = "Filter images by name."
}

variable "sort" {
  type        = string
  default     = ""
  description = "Sort order, e.g. name:asc."
}

variable "page" {
  type        = number
  default     = 0
  description = "Page number (0-based)."
}

variable "size" {
  type        = number
  default     = 20
  description = "Number of items per page."
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `repository_id` (String) The ID of the repository to list images from.

### Optional

- `filter` (Block List) Filter (see [below for nested schema](#nestedblock--filter))
- `name` (String) Filter images by name.
- `page` (Number) Page number (0-based).
- `size` (Number) Number of items per page.
- `sort` (String) Sort order, e.g. `name:asc`.

### Read-Only

- `ids` (List of String) List of image IDs.

<a id="nestedblock--filter"></a>
### Nested Schema for `filter`

Required:

- `name` (String) Filtering target name
  - example: name
- `use_regex` (Boolean) Enable regex match for values
  - example: true
- `values` (List of String) Filtering values. Each matching value is appended. (OR rule)
  - example: ['values']