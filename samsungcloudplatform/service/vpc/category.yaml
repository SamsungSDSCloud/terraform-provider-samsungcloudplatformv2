category:
  vpc_vpc_endpoint: VPC
  vpc_vpc_endpoints: VPC
  vpc_internet_gateway: VPC
  vpc_internet_gateways: VPC
  vpc_nat_gateway: VPC
  vpc_nat_gateways: VPC
  vpc_publicip: VPC
  vpc_publicips: VPC
  vpc_port: VPC
  vpc_ports: VPC
  vpc_subnet: VPC
  vpc_subnets: VPC
  vpc_vpc: VPC
  vpc_vpcs: VPC
  vpc_private_nat: VPC
  vpc_private_nats: VPC
  vpc_private_nat_ip: VPC
  vpc_private_nat_ips: VPC
  vpc_vpc_peering: VPC
  vpc_vpc_peerings: VPC
  vpc_transit_gateway: VPC
  vpc_transit_gateways: VPC
  vpc_transit_gateway_vpc_connection: VPC
  vpc_transit_gateway_vpc_connections: VPC
  vpc_vpc_peering_rule : VPC
  vpc_vpc_peering_rules : VPC
  vpc_transit_gateway_rule : VPC
  vpc_transit_gateway_rules : VPC
  vpc_vpc_peering_approval : VPC
